# WinGo API workspace

- FastAPI service targeting Python 3.12.
- Render deployment is configured through `render.yaml`.
- Keep upstream records normalized before returning them from the API.
- Run `python -m compileall main.py` as a quick validation after Python changes.
